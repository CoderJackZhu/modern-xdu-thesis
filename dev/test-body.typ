// 测试：正文框架（标题层级 / 分章编号 / 页眉切换 / 每章奇数页起）
// 标题编号由模板生成，这里不写编号
#import "../lib.typ": documentclass

#let (doc, mainmatter) = documentclass(
  degree: "professional",
  info: (title: ("测试",), author: "张三"),
)
#show: doc
#show: mainmatter.with(header-title: "西安电子科技大学硕士学位论文")

= 第一章 绪论

第一节的正文内容。测试固定行距是否成立，以及首行缩进两字符的效果。这里放足够长的中文
内容让段落自动换行，从而可以测量相邻基线的间距是否恒为二十磅。继续补充一些文字以保证
至少能排满若干行，方便验证版面。再补几句凑够行数。

== 研究背景

本节正文。毫米波大规模 MIMO 系统的信道估计是关键技术之一，需要处理高维度、稀疏性与
计算复杂度等问题。下面给出一段足够长的文字，用于测量节标题到正文的间距以及正文行距。

=== 国内研究现状

小节正文内容。同样需要若干行文字来验证小节标题的缩进与段前段后间距是否与规格一致。

==== 第一个要点

四级标题的正文。

= 第二章 方法

第二章的正文。用于验证章标题从奇数页开始、页眉切换到新章名，以及图表的章内编号重置。

== 系统模型

#figure(rect(width: 4cm, height: 2cm), caption: [系统框图])
#figure(table(columns: 2, [参数], [取值], [频率], [28 GHz]), caption: [仿真参数])
